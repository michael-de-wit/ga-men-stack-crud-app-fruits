<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fruit show page</title>
</head>
<body>
    <h1>Here's your fruit: <%= fruit.name %></h1>
    <% if(fruit.isReadyToEat === true) { %>
        <h2>It is ready to eat</h2>
    <% } else { %>
        <h2>It is not ready to eat</h2>
    <% } %>
    <a href="/fruits">Back to fruits</a>
    <form action="/fruit/<%= fruit._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= fruit.name %></button>
    </form>
    <a href="/fruits/<%= fruit._id %>/edit">Edit <%= fruit.name %></a>
    <a href="/fruits">Back to fruits</a>
</body>
</html>