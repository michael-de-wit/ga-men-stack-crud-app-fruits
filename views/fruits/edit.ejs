<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit fruit</title>
</head>
<body>
    <h1>Edit <%= fruit.name %></h1>
    <form>
        <label>Name:</label>
        <input type="text" value="<%= fruit.name %>"/>
        <label>Read to eat?</label>
        <input type="checkbox" 
        <% if(fruit.isReadyToEat) { %>
            checked
        <% } %>
        >
        <button type="submit">Update fruit</button>
    </form>
    <a href="/fruits">Back to fruits</a>
</body>
</html>